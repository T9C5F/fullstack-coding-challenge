version: '3.1'

services:
  mongo:
    image: mongo:5
    restart: always

  api:
    build: ./backend/
    restart: always
    ports:
      - 3000:3000
    environment:
      MONGODB_URL: mongodb://mongo:27017/

  frontend:
    build: ./frontend/
    restart: always
    ports:
      - 8080:80
